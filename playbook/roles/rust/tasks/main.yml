---
- name: Install Rust toolchain
  shell: curl https://sh.rustup.rs -sSf | sh -s -- -y --no-modify-path
  args:
    creates: "$HOME/.cargo/bin/rustup"

# - name: Install racer
#   shell: $HOME/.cargo/bin/cargo install racer
#   args:
#     creates: "$HOME/.cargo/bin/racer"
#
# - name: Install rustfmt
#   shell: $HOME/.cargo/bin/cargo install rustfmt
#   args:
#     creates: "$HOME/.cargo/bin/rustfmt"
